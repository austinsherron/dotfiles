#!/usr/bin/env bash


{{ $target := "/etc/profile.d" }}
{{ $shared_paths := (print .chezmoi.homeDir "/.config/shell/bash/helpers/shared_paths.sh") }}

# paths.sh hash: {{ include $shared_paths | sha256sum }}

if [[ ! -d /etc/profile.d ]]; then
    sudo mkdir {{ $target }}
fi

sudo cp {{ $shared_paths }} {{ print $target "/shared_paths.sh" }}

